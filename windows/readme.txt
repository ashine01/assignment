Chrome:
If you are using Chrome version 103, you can directly run the "autoTest.py" under the "windows" folder; otherwise, you must replace the webdriver with the correct version before running.
Microsoft Edge
If you are using Microsoft Edge in version 103.0.1264.44, you can directly run the "autoTest.py" under the "windows" folder; otherwise, you must replace the webdriver with the correct version before running.